# Repository instructions

Read `docs/RULES.md` before changing this repository; it is the authoritative project contract.

Fix causes instead of symptoms. Keep changes native, strongly typed, modular, and explicit. Add focused regression coverage for every corrected defect, keep `docs/TODO.md` current, and run `pnpm verify` before completion.
