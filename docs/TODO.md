# TODO

- [ ] Evaluate Responses WebSocket in an isolated module and adopt it only after
  proving ordering, reconnection, and cancellation parity with SSE.
- [ ] Configure the four signing secrets in `RELEASE.md` when the Authenticode
  certificate becomes available.
- [ ] Re-evaluate the stable Solid compiler pipeline when Solid 2 and its
  supported OXC-based Vite integration are stable; migrate only after a
  reproducible build benchmark and full compatibility verification.
